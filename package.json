{
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --watch app.js",
    "format": "prettier * -w -u --no-error-on-unmatched-pattern --log-level error",
    "lint": "eslint",
    "check": "npm run format && npm run lint"
  },
  "dependencies": {
    "connect-pg-simple": "^10.0.0",
    "ejs": "^6.0.1",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "express-validator": "^7.3.2",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.6.0",
    "globals": "^17.7.0"
  }
}
